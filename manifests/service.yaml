apiVersion: v1
kind: Service
metadata:
  name: bug-tracker-service
  namespace: devsecops
spec:
  type: NodePort
  selector:
    app: bug-tracker
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007